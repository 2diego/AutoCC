# Archivo guía principal.
# Copiar según uso:
# - .env.development.example -> .env.development (desarrollo diario)
# - .env.migrations.example  -> .env.migrations  (comandos TypeORM migration:*)
#
# Producción (Render): usar variables del panel, no un archivo .env subido al repo.